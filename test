#0. 그래프 생성
#1. 팀원별 (일련번호, 매출액) 으로 분리
#2. 팀별 grpah에서 팀장 및 부하들의 최솟값 2개를 선택해서 선정 후 하나씩 탐색 수행 
#3. 팀원별 bool활용해서 갔던 경로 폐쇄 
#4. 나온값 해결

def solution(sales, links):
    graph = [] #내부 리스트(팀번호(A,B)): [[(팀장 일련번호, 매출액) , (직원1 일련번호, 매출액)...]...]
    
    #팀장 튜플 추가 후 직원 튜플 추가
    tmp_lst = [] 
    for lst in links:
        if lst[0] not in tmp_lst:
            graph.append([(lst[0] , sales[lst[0]-1])])
            tmp_lst.append(lst[0])
    
    for lst in links:
        for team in graph:
            if team[0][0] == lst[0]:
                graph[team].append((lst[1] , sales[lst[1]-1]))
    
    team_bool = [True for i in range(len(graph))]
    
    rep_bool = [a, b for ]
    
            
    return answer
