
def solution(n, wires):
    
    count = {} 
    for i in wires:
        for j in i:
            count[j] = count.get(j,0)+1 #get: 뒤에는 디폴트값\
        max_num = max(count, key = count.get)
        max_keys = [k for k, v in count.items() if v == max_num]
    lst_new = wires     
    for i in range(len(wires)):
        if max_num  in wires[i]:
            del lst_new[i]
            answer = clear_func(lst_new,max_num)
            
    return answer

def clear_func(lst_new, max_num): #통해서 제거할 예정
    while True
