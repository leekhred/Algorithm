def solution(n, s, a, b, fares):
    global graph
    global A_RouteLst
    global B_RouteLst
    
    
    graph = [{} for _ in range(len(fares))] # [{} , {} ,{} .... 이런 형태?]
    for st , fin , fare in range(len(fares)):
        graph[st][fin] = fare
        graph[fin][st] = fare
    A_RouteLst = []
    B_RouteLst = []
    
    search(s, graph[s] , [])

def search(head,graph_n , tmp_lst): #순환함수 search 매개변수는 graph내부의 딕셔너리
    for key, value in graph_n.items():
        if key not in tmp_lst[:-1]: #마지막 값은 더할 sum이 들어가 있음
            tmp_lst.insert(0, )
            tmp_lst.append(
            search(key , graph[key] , tmp_lst)
        
        
    
    
    return answer 
