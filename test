def solution(triangle):
    answer = 0
    sum_num = []
    sum_num = dfs(triangle, triangle[0] ,0 , 5, sum_num)
    
    return answer
    
def dfs(triangle, index_pre, high_pre , limit, sum_num): #배열, 현재 인덱스, 높이 , 높이한계
    if high_pre == limit-1: #말단 노드
        sum_num += triangle[high_pre][index_pre]
        
