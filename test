tmp_lst = []
def solution(n , wires):
    graph = [[] for _ in range(n+1) ]
    for u , v in wires:
        graph[u].append(v)
        graph[v].append(u)
    
    sum = 0
    answer = None
    for v1, v2 in wires:
        tmp_lst2 = list(tmp_lst)
        tmp_lst.append(v1) #v1삭제 v2를 기준으로 탐색
        dps(v2 , graph)
    
def dps(v2 , graph):
    for i in range(graph[v2])
        if i not in tmp_lst:
            tmp_lst.append(i)
            
        
        
        
    
