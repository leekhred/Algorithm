"""문제분석
1. wire 가 가장 많이 연결되어 있는 송전탑을 탐색
2. max 송전탑 기준으로 wire 하나씩 제거
3. """

def solution(n, wires):
    
    count = {} 
    for i in wires:
        for j in i:
            count[j] = count.get(j,0)+1 #get: 뒤에는 디폴트값\
        max_num = max(count, key = count.get)
    lst_new = wires     
    for i in range(len(wires)):
        if max_num  in wires[i]:
            del lst_new[i]
            answer = clear_func(lst_new,max_num)
            
    return answer

def clear_func(lst_new, max_num): #순환함수 통해서 제거할 예정
    
    
    
